{
  "name": "home-hub",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "server": "bun run --watch server/src/index.ts",
    "client": "cd client && ng serve"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
